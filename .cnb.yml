main:
  push:
    - name: Build and Deploy EdgeOne Pages (main)
      runner:
        tags: cnb:arch:amd64
        cpus: 16
      docker:
        image: node:20
        volumes:
          - /root/.local/share/pnpm/store:copy-on-write
      imports:
        - https://cnb.cool/sorkai/vue-pure-admin-max-secrets/-/blob/main/envs.yml
      env:
        NODE_OPTIONS: --max-old-space-size=8192
        VITE_PUBLIC_PATH: /
        VITE_ROUTER_HISTORY: hash
        VITE_CDN: "false"
        VITE_COMPRESSION: none
        VITE_PROXY_TARGET: https://aiedu-api.intelledu.cn
      stages:
        - name: Prepare pnpm
          script: |
            set -eu
            corepack enable
            corepack prepare pnpm@9 --activate
            node -v
            pnpm -v
            pnpm config set store-dir /root/.local/share/pnpm/store

        - name: Install dependencies
          script: |
            set -eu
            pnpm install --no-frozen-lockfile

        - name: Build
          script: |
            set -eu
            pnpm test:contracts
            pnpm typecheck
            pnpm build

        - name: Verify build output
          script: |
            set -eu
            test -d dist
            test -f dist/index.html

        - name: Deploy to EdgeOne Pages
          script: |
            set -eu
            if [ -z "${EDGEONE_API_TOKEN:-}" ]; then
              echo "EDGEONE_API_TOKEN is not configured. Import it from a CNB secret repository before deploying." >&2
              exit 1
            fi
            npx --yes edgeone pages deploy ./dist -n aiedu -t "$EDGEONE_API_TOKEN" -e production

dev:
  push:
    - name: Build and Deploy EdgeOne Pages (dev)
      runner:
        tags: cnb:arch:amd64
        cpus: 16
      docker:
        image: node:20
        volumes:
          - /root/.local/share/pnpm/store:copy-on-write
      imports:
        - https://cnb.cool/sorkai/vue-pure-admin-max-secrets/-/blob/main/envs.yml
      env:
        NODE_OPTIONS: --max-old-space-size=8192
        VITE_PUBLIC_PATH: /
        VITE_ROUTER_HISTORY: hash
        VITE_CDN: "false"
        VITE_COMPRESSION: none
        VITE_PROXY_TARGET: https://aiedu-api.intelledu.cn
      stages:
        - name: Prepare pnpm
          script: |
            set -eu
            corepack enable
            corepack prepare pnpm@9 --activate
            node -v
            pnpm -v
            pnpm config set store-dir /root/.local/share/pnpm/store

        - name: Install dependencies
          script: |
            set -eu
            pnpm install --no-frozen-lockfile

        - name: Build
          script: |
            set -eu
            pnpm test:contracts
            pnpm typecheck
            pnpm build

        - name: Verify build output
          script: |
            set -eu
            test -d dist
            test -f dist/index.html

        - name: Deploy to EdgeOne Pages
          script: |
            set -eu
            if [ -z "${EDGEONE_API_TOKEN:-}" ]; then
              echo "EDGEONE_API_TOKEN is not configured. Import it from a CNB secret repository before deploying." >&2
              exit 1
            fi
            npx --yes edgeone pages deploy ./dist -n aiedu-dev -t "$EDGEONE_API_TOKEN" -e production

agent:
  push:
    - name: Build and Deploy EdgeOne Pages (agent)
      runner:
        tags: cnb:arch:amd64
        cpus: 16
      docker:
        image: node:20
        volumes:
          - /root/.local/share/pnpm/store:copy-on-write
      imports:
        - https://cnb.cool/sorkai/vue-pure-admin-max-secrets/-/blob/main/envs.yml
      env:
        NODE_OPTIONS: --max-old-space-size=8192
        VITE_PUBLIC_PATH: /
        VITE_ROUTER_HISTORY: hash
        VITE_CDN: "false"
        VITE_COMPRESSION: none
        VITE_PROXY_TARGET: https://aiedu-api.intelledu.cn
      stages:
        - name: Prepare pnpm
          script: |
            set -eu
            corepack enable
            corepack prepare pnpm@9 --activate
            node -v
            pnpm -v
            pnpm config set store-dir /root/.local/share/pnpm/store

        - name: Install dependencies
          script: |
            set -eu
            pnpm install --no-frozen-lockfile

        - name: Build
          script: |
            set -eu
            pnpm test:contracts
            pnpm typecheck
            pnpm build

        - name: Verify build output
          script: |
            set -eu
            test -d dist
            test -f dist/index.html

        - name: Deploy to EdgeOne Pages
          script: |
            set -eu
            if [ -z "${EDGEONE_API_TOKEN:-}" ]; then
              echo "EDGEONE_API_TOKEN is not configured. Import it from a CNB secret repository before deploying." >&2
              exit 1
            fi
            npx --yes edgeone pages deploy ./dist -n aiedu-agent -t "$EDGEONE_API_TOKEN" -e production
